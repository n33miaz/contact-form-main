function enviar() 
{
    const inputs = document.querySelectorAll("#primeiro_nome, #ultimo_nome, #email, #solicit_geral, #solicit_suporte, #mensagem, #termos");

    if (inputs.values !== null)
    {

    }
    else
    {
        alert("Mensagem Enviada!");
    }
}
